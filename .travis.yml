language: python
sudo: false
python:
- '2.7'
- '3.4'
- '3.5'
- '3.6'

matrix:
  include:
    - sudo: required
      services:
        - docker
      env: PIP=pip
#    - os: osx
#      language: generic
#      env: PIP=pip2

script:
  - $PIP install -U --only-binary=numpy numpy cibuildwheel
  - cibuildwheel --output-dir wheelhouse